Task: Gather implementation details for Student Admin authentication feature

1. Django Authentication
* The project is using Django's built-in User model (`django.contrib.auth.models.User`). There is no custom `AUTH_USER_MODEL` defined in `settings.py`.
* Authentication Backend: The default Django ModelBackend is configured.

2. Existing Website Admin Login
* View Location: `edusetin_app.views.admin_login`
* URL Path: `/login/`
* Template: `authenticate/login.html`
* Flow: Validates username/password using Django's `authenticate()`. If the user is authenticated and `user.is_staff` is True, it logs them in via `login()` and redirects to `admin-dashboard`.
* Middleware/Decorators: The dashboard and admin views are protected using the standard Django `@login_required(login_url="admin_login")` decorator.

3. Existing Website Admin Dashboard
* URL Structure: `/admin-dashboard/`
* App Name: `edusetin_app`
* Base Template: Renders `admin_pages/admin-dashboard.html` (which likely extends an admin base layout within `templates/admin_pages/`).
* Navigation/Sidebar: Implemented within the HTML templates (likely in a sidebar or base template included by `admin-dashboard.html`).

4. Current User Management
* There is currently NO custom user management module or UI to create admin users (the default Django `/admin/` path is commented out in `edusetin/urls.py`).
* Admins cannot currently create users from the custom dashboard.
* There are no existing role/group/permission models apart from the default Django `User` fields (e.g. `is_staff`, `is_superuser`).

5. Project Structure
* Relevant Apps: `edusetin_app` is the primary and only custom app handling models and views.
* App for Student Admin Authentication: `edusetin_app` (or a dedicated app if the project needs strict decoupling, but currently everything is in `edusetin_app`).
* App for UserProfile: `edusetin_app`

6. Current Models
* Student Model: There is currently NO dedicated `Student` model. There are models like `Application` (full_name, email, phone, qualification, course) and `ChatbotUser`, which act as student inquiries/applications.
* Profile/Role Models: None exist. 
* UserProfile: Confirmed that `UserProfile` does NOT currently exist.

7. URL Configuration
* Project `urls.py` (`edusetin/urls.py`):
  ```python
  urlpatterns = [
      # path("admin/", admin.site.urls),
      path('', include('edusetin_app.urls')),
  ]
  ```
* Dashboard & Auth `urls.py` (`edusetin_app/urls.py`):
  ```python
  path("login/", views.admin_login, name="admin_login"),
  path("admin-logout/", views.admin_logout, name="admin_logout"),
  path("admin-dashboard/", views.admin_dashboard, name="admin-dashboard"),
  ```

8. Templates
* Login Template Path: `templates/authenticate/login.html`
* Dashboard Template Path: `templates/admin_pages/admin-dashboard.html`
* Layout Reuse: The Student Admin dashboard can potentially reuse the same base layout (e.g., header/sidebar layout from `admin_pages`), but it should conditionally hide navigation items that are only meant for the Website Admin.

9. Security / Access Control
* Restriction: Website Admin access is restricted purely by checking `user.is_staff` during login and using the `@login_required(login_url="admin_login")` decorator on admin views.
* Mechanisms Used:
  - `login_required`: YES
  - Custom Decorators: NO
  - Middleware: Standard Django Auth/Session middleware.
  - Django Permissions: NO (not used functionally in custom views).

10. Desired Implementation Feasibility & Recommended Approach
Based on the current codebase, the simplest approach is to add role-based access directly within the `edusetin_app`.

* Separate Student Admin Login & Dashboard:
  - Add views `student_admin_login` and `student_admin_dashboard` in `edusetin_app/views.py`.
  - Add URLs:
    `path("student-admin/login/", views.student_admin_login, name="student_admin_login")`
    `path("student-admin/dashboard/", views.student_admin_dashboard, name="student_admin_dashboard")`

* UserProfile Model (in `edusetin_app/models.py`):
  ```python
  class UserProfile(models.Model):
      ROLE_CHOICES = (
          ('WEBSITE_ADMIN', 'Website Admin'),
          ('STUDENT_ADMIN', 'Student Admin'),
      )
      user = models.OneToOneField(User, on_delete=models.CASCADE)
      role = models.CharField(max_length=20, choices=ROLE_CHOICES, default='WEBSITE_ADMIN')
  ```

* User Management for Website Admin:
  - Create views (`create_student_admin`, `edit_student_admin`, `deactivate_student_admin`) protected by a new decorator ensuring `role == 'WEBSITE_ADMIN'`.
  - Add an interface in the Website Admin dashboard to manage these users.

* Access Control for Existing Functionality:
  - Create a custom decorator `@website_admin_required` (replacing or wrapping `@login_required`) for existing views to ensure `user.userprofile.role == 'WEBSITE_ADMIN'`.
  - Create `@student_admin_required` for the Student Admin views to restrict them to managing applications and student data only.
  - The existing `admin_login` view should be updated to check `user.userprofile.role == 'WEBSITE_ADMIN'`, while `student_admin_login` checks for `STUDENT_ADMIN`.

This approach ensures zero disruption to the existing schema while clearly segregating the two admin roles using a simple OneToOne profile model.
